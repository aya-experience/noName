import React from 'react';
import { shallow } from 'enzyme';
import Highlight from './';

describe('Highlight', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallow(<Highlight />);
  });

  it('should works', () => {

  });
});

import React from 'react';
import { shallow } from 'enzyme';
import Highlight from './index';

describe('Highlight', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallow(<Highlight />);
  });

  it('should works', () => {

  });
});
